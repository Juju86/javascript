export default class {
  /**
   * Represents a Object.
   * @formatData
   * @param {}
   */
  static _formatData () {
    return '_formatData'
    // return this._temp.valueOf().map(() => {
    // Creation de data
    // })
  }
}
